# Finovate Audit Nexus AI - Requirements (Cross-platform / CI / Linux)
# Enterprise AI Financial Audit & Intelligence Platform
#
# USAGE:
#   pip install -r requirements.txt              (Linux / CI / macOS)
#   pip install -r requirements-windows.txt       (Windows Desktop build)
#
# NOTE: requirements-windows.txt has platform-specific deps (torch, langchain,
# pywin32) and pins exact versions for reproducible Windows builds.
#
# Heavy ML deps (tensorflow, torch, scikit-learn, opencv) are optional:
#   pip install "finovate-audit-nexus-ai[ml]"   or install from pyproject.toml

# Core Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
pydantic>=2.5.3
pydantic-settings>=2.1.0

# Database
sqlalchemy>=2.0.25
alembic>=1.13.1
psycopg2-binary>=2.9.9
aiosqlite>=0.19.0
asyncpg>=0.29.0

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.2
cryptography>=41.0.0

# AI & Machine Learning (lightweight)
openai>=1.10.0
anthropic>=0.8.1
google-generativeai>=0.3.0
groq>=0.4.0

# Data Processing
pandas>=2.2.0
openpyxl>=3.1.2
numpy>=1.26.0
aiohttp>=3.9.0
pdfplumber>=0.10.0
python-docx>=1.0.0
xlrd>=2.0.0

# Reporting (PDF, Excel)
reportlab>=4.0.8
jinja2>=3.1.2
weasyprint>=60.0
markdown>=3.5.0
plotly>=5.18.0

# Utilities
python-dotenv>=1.0.0
aiofiles>=23.2.1
requests>=2.31.0
zeep>=4.2.1
pyodbc>=5.1.0
tenacity>=8.2.0
prometheus-client>=0.19.0
structlog>=23.0.0
rich>=13.0.0

# Logging
loguru>=0.7.2

# Caching & Task Queue
redis>=5.0.0

# WebSocket (via FastAPI/Starlette — built-in support)

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
httpx>=0.26.0

# Development & Code Quality
ruff>=0.1.0
black>=23.0.0
mypy>=1.6.0
pre-commit>=3.5.0

# Security Scanning
safety>=2.3.0
bandit>=1.7.0
